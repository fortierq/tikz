\documentclass[convert={outfile=\jobname.png}]{standalone}
\usepackage{base}
\usepackage{draw}

\begin{document}

\begin{tikzpicture}[]
    % nim game
    \foreach \x in {1,...,6} {
        \node (\x) at (-1.5*\x,0) {$\x$};
    }
    \foreach \x in {1,...,5} {
        \draw[->, >=latex] (\the\numexpr1+\x) -> (\x);
    }
    \foreach \x in {1,...,4} {
        \draw[->, >=latex] (\the\numexpr2+\x) to [bend left] (\x);
    }
    \foreach \x in {1,...,3} {
        \draw[->, >=latex] (\the\numexpr3+\x) to [bend right] (\x);
    }
\end{tikzpicture}

\end{document}
