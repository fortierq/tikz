\documentclass[convert={outfile=\jobname.png}]{standalone}

\usepackage{tikz}

\begin{document}

\begin{tikzpicture}[every node/.style={draw}]
\node {$2$}
child { node {$1$};
child { node {$0$};
child[missing] {}
child[missing] {}
}
child[missing] {}
}
child { node {$3$};
child[missing] {}
child { node {$5$};
child[missing] {}
child[missing] {}
}
};
\end{tikzpicture}
\end{document}