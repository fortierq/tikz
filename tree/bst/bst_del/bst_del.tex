\documentclass[convert={outfile=\jobname.png}]{standalone}
\usepackage{draw}
\usepackage{base}

\begin{document}
    \def\dist{5.5}

    \begin{tikzpicture}[cross/.style={path picture={ 
        \draw[black]
      (path picture bounding box.south east) -- (path picture bounding box.north west) (path picture bounding box.south west) -- (path picture bounding box.north east);
      }},level 1/.style={sibling distance=\dist cm}, level 2/.style={sibling distance=.4*\dist cm}, level 3/.style={sibling distance=.25*\dist cm}, every node/.style = {vertex, minimum size=.6cm}]
    \node[cross] (r) {}
    child { node {1} 
        child { node {-2} }
        child { node {3} 
            child { node {2} }
            child { node[] (f) {4} } } }
    child { node {11} 
        child { node {8} 
            child { node {7} } child[missing] {node {}}}
        child { node {23} } };
    \draw[->, >=latex, thick] (f) to[bend right = 10] (r);
    \end{tikzpicture}

\end{document}
