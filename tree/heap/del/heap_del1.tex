\documentclass[convert={outfile=\jobname.png}]{standalone}

\usepackage{base}
\usepackage{draw}

\begin{document}

\begin{tikzpicture}[cross/.style={path picture={ 
    \draw[black]
  (path picture bounding box.south east) -- (path picture bounding box.north west) (path picture bounding box.south west) -- (path picture bounding box.north east);
  }}, every node/.style = {vertex}, level 1/.style={sibling distance=3.7cm}, level 2/.style={sibling distance=1.8cm}, , level 3/.style={sibling distance=.9cm}]
    \node[cross] (42) {42}  
    child { node {7}
        child { node {5} 
            child { node {3} }
            child { node {-8} } }
        child { node {2} 
            child { node {-2} } 
            child { node {0} } } }
    child { node {5}
        child { node {2}
            child { node (1) {1} } 
            child[missing] }
        child { node {3} } };

    \draw[arc] (1) to[bend left=10] (42);
\end{tikzpicture}

\end{document}
