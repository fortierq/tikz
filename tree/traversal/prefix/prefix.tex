\documentclass[convert={outfile=\jobname.png}]{standalone}
\usepackage{draw}

\begin{document}

\begin{tikzpicture}[every node/.style={vertex}, level 1/.style={sibling distance=3.5cm}, level 2/.style={sibling distance=1.8cm}, level 3/.style={sibling distance=1cm}]
    \node (r) {$r$}
    child {
        node (g) {9}
        child {
            node (gg) {5}
        }
        child {
            node (gd) {0}
            child {node (gdg) {3}}
            child[missing] {}
        }
    }
    child {
        node (d) {2}
        child {node (dg) {6}}
        child {node (dd) {8}}
    };
    % \def\s{0.5};
    % \coordinate (sx) at (\s, 0);
    % \coordinate (sy) at (0, \s);
    % \coordinate (sxy) at (-0.7*\s, 0.7*\s);
    % \draw[arc] ($(r) + (sxy)$) to ($(g) + (sxy)$);
    \draw[arc, semithick] (r) edge (g) (g) edge (gg) (gg) edge (gd) (gd) edge (gdg);
\end{tikzpicture}

\end{document}
