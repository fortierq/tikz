\documentclass[convert={outfile=\jobname.png}]{standalone}
\usepackage{draw}

\begin{document}
    \def\size{4}
    \def\a{.2}
	\begin{tikzpicture}
        \draw[->] (0, 0) node[point, label=below:$r$] (r) {} --++ (\size, 0) node[label=right:$x$] {};
        \draw[->] (r) --++ (0, \size) node[label=above:$y$] {};
        \draw[<->] (r) to node[sloped, above] {$d$} (.7*\size, .7*\size) node[point, label=right:$p$] {};
        \draw[->] (\a*\size,0) arc (0:45:\a*\size);
        \node at (.15+\a*\size, .5*\a*\size) {$a$};
    \end{tikzpicture}

\end{document}
