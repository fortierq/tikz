\documentclass[convert={outfile=\jobname.png}]{standalone}
\usepackage{base}

\begin{document}
Si $A$ n'a pas de variable liée en commun avec $t$ :
\begin{prooftree}
  \hypo{\Gamma \vdash \forall x\, A}
  \infer1[$\forall_e$]{\Gamma \vdash A[x:=t]}
\end{prooftree}
\end{document}
