\documentclass[convert={outfile=\jobname.png}]{standalone}
\usepackage{base}

\begin{document}
Si $X$ n'a pas de variable liée en commun avec $t$ :
\begin{prooftree}
  \hypo{\Gamma \vdash \forall x\, X}
  \infer1[$\forall_e$]{\Gamma \vdash X[x:=t]}
\end{prooftree}
\end{document}
