\documentclass[convert={outfile=\jobname.png}]{standalone}
\usepackage{base}

\begin{document}

\begin{tikzpicture}
    \def\n{5}
    \foreach \j in {1, 3}
        \draw[fill=gray!50] (0, \j) rectangle (\n, \j + 1);
    \foreach \i in {0, ..., \n} {
        \draw (\i, 0) -- (\i, \n);
        \draw (0, \i) -- (\n, \i);
    }
\end{tikzpicture}

\end{document}
